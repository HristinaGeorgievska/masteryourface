# ── Contentful (server-side only — used by /api/courses and /api/showcase) ──
# Set these in Vercel dashboard for production. For local dev, use `vercel dev`.
CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=

# ── SMTP (server-side only — used by /api/contact) ─────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
CONTACT_EMAIL=

# ── Anti-spam HMAC token (server-side only — used by /api/token + /api/contact)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FORM_TOKEN_SECRET=
